# Environment template (NO secrets)
#
# Copy this file to ONE of the following (these are gitignored):
# - .env.local                 (default)
# - .env.staging.local         (when ENV=staging)
# - .env.production.local      (when ENV=production)
#
# The app expects these keys:
#
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# Gateway Configuration
CWE_MVR_API_URL=
CWE_MVR_API_KEY=

